// import type { PropsSchema as ContainerProps } from "./container.validation";
// import { ContainerProvider } from "./container.provider";

// type AtlasConfig = ContainerProps;

// export function AtlasProviders({ children, config }: React.PropsWithChildren & { config: AtlasConfig }) {
//   return (
//     <ContainerProvider platform={config.platform} userRole={config.userRole}>
//       {children}
//     </ContainerProvider>
//   );
// }
